# -*- coding: utf-8 -*-
"""
Created on: 18.06.25 14:42
@author: Dmytro Nikolaenko (nikodim74@gmail.com)
@user: d_nikolaenko
@project name: Python3_dev_courses
@file: temp.py
"""

# Програма приймає речення, рахує частоти унікальних слів,
# а також обчислює загальну вартість запасів товарів зі складу.

# Ввід речення з клавіатури
sentence = input("Введіть речення: ")

# Перетворення речення у список слів у нижньому регістрі
words = sentence.lower().split()

# Створюємо порожній словник для підрахунку унікальних слів
word_count = {}

# Проходимо по кожному слову зі списку
for word in words:
    # Очищуємо слово від розділових знаків (.,!? та ін.)
    word = word.strip(".,!?\"'")

    # Якщо слово не порожнє (тобто не було лише розділовим знаком)
    if word:
        # Якщо слово вже є в словнику — додаємо 1 до лічильника
        # Якщо нема — додаємо його зі значенням 1
        word_count[word] = word_count.get(word, 0) + 1

# Виводимо результат підрахунку унікальних слів
print("\n📚 Унікальні слова та кількість їх вживань:")
print(word_count)

# Словник із кількістю товарів на складі
stock = {
    "banana": 6,
    "apple": 0,
    "orange": 32,
    "pear": 15
}

# Словник із цінами за одиницю кожного товару
prices = {
    "banana": 4,
    "apple": 2,
    "orange": 1.5,
    "pear": 3
}

# Створюємо порожній словник для зберігання сумарної вартості кожного товару
inventory_value = {}

# Обчислюємо сумарну вартість кожного товару як: кількість * ціна
for item in stock:
    # Беремо кількість із stock та ціну з prices
    # Якщо ціна не знайдеться в словнику — беремо 0 (щоб уникнути помилки)
    inventory_value[item] = stock[item] * prices.get(item, 0)

# Виводимо словник із загальною вартістю кожного типу товару
print("\n💰 Загальна вартість за типами товарів:")
print(inventory_value)

